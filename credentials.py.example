# Copiez ce fichier en `credentials.py` et remplissez vos identifiants locaux.
# IMPORTANT: ne commitez PAS le fichier `credentials.py` dans le dépôt.

# Exemple :
username = ""
password = ""
link = ""

# Usage : importez les variables depuis votre script :
# try:
#     import credentials
#     username = credentials.username
#     password = credentials.password
# except ImportError:
#     # Fallback aux variables d'environnement si vous préférez
#     import os
#     username = os.environ.get('MOODLE_USER')
#     password = os.environ.get('MOODLE_PASS')